# Каталог: VTBL.Restrict.Loader.Application

## Назначение
Сценарии загрузки (upload / retry notify), порты (interfaces), observability scopes, опции хранения.

## Файлы

### Abstractions/IUploadBatchStore.cs
**Классы:**
- `IUploadBatchStore` — учёт UploadBatch и статус notify

### Abstractions/IListTypeReadStore.cs
**Классы:**
- `IListTypeReadStore` — чтение справочника ListType

### Abstractions/IFileShareStore.cs
**Классы:**
- `IFileShareStore` — запись файла as-is на шару

### Abstractions/IUploadPathBuilder.cs
**Классы:**
- `IUploadPathBuilder` — построение пути назначения

### Abstractions/IUploadNotifier.cs
**Классы:**
- `IUploadNotifier` — публикация события загрузки (RMQ / InMemory)

### Uploads/UploadRestrictFileCommand.cs
**Классы:**
- `UploadRestrictFileCommand` — валидация оболочки → WriteAsIs → UploadBatch → publish

### Uploads/RetryUploadNotificationCommand.cs
**Классы:**
- `RetryUploadNotificationCommand` — повтор RMQ без повторной записи файла

### Uploads/UploadErrorCodes.cs
**Классы:**
- `UploadErrorCodes` — коды ошибок upload/retry

### Observability/OperationLogScope.cs
**Классы:**
- `OperationLogScope` — BeginUpload / BeginPublish / BeginRetry; ключи operation, correlationId, listType, errorCode

### Options/RestrictStorageOptions.cs
**Классы:**
- `RestrictStorageOptions` — RemoteRoot и параметры шары

## Зависимости
- `VTBL.Restrict.Loader.Domain` — enums/validators upload
